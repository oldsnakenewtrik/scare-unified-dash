[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c \"python -m uvicorn src.api.super_simple:app --host 0.0.0.0 --port ${PORT:-8000}\""
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
