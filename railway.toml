[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c \"python -m uvicorn src.api.main:app --host 0.0.0.0 --port ${PORT:-8000}\""
healthcheckPath = "/api/cors-test"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
